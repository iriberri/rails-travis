language: node_js
node_js:
  - 9.11.1
addons:
  postgresql: "9.4"
services:
  - postgresql
before_install:
  - gem install bundler
  - bundle install
cache:
  bundler: true
  directories:
    - ~/.npm
    - ~/.cache
    - "node_modules"
install:
  - npm install
before_script:
  - rails server &
  - psql -c 'create database MY_APP_test;' -U postgres
script:
  - sleep 30
  - bundle exec rake db:seed
  - $(npm bin)/cypress run